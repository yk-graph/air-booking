services:
  migrate-stg:
    image: ghcr.io/yk-graph/air-booking-migrator:staging
    container_name: air-booking-migrate-stg
    env_file:
      - .env.staging
    restart: 'no'

  web-stg:
    image: ghcr.io/yk-graph/air-booking-web:staging
    container_name: air-booking-web-stg
    ports:
      - '4210:4000'
    env_file:
      - .env.staging
    environment:
      - NODE_ENV=production
    restart: unless-stopped
